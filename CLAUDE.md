# げんつきマップ ｜ 作業のしかた

作者は非技術者。**説明は日本語で、専門用語はその場で言い換える。**

## 公開する前に必ず通す

```
./tools/check.sh
```

これが落ちたら公開しない。中身は3つ。

1. `docs/app.js` の文法
2. `tools/audit_layers.py` … 規制の種別が**使う側すべて**に載っているか
3. キャッシュ避けの版が `app.js` と `style.css` で揃っているか

## ひとつ直したら、その事実を使っている側を全部たどる

このアプリは同じ事実を何か所にも持っている。**指摘された1か所だけ直すと、
残りが古いまま矛盾する。**実際に2回起きた（下の「過去の取りこぼし」）。

直す前に、その事実の**使う側の一覧**を作ってから手をつける。手順は5つ。

1. **使う側を数え上げる。** 変える事実（フラグ・件数・種別・状態）を読んでいる場所を
   `grep` で全部出し、下の表と突き合わせる。
2. **表示以外も直す。** この地図では **数える・避ける・読み上げる・記録する** 側を
   忘れやすい。使われていない側があったら、「使っていない」ではなく
   **壊れている**可能性を疑う（`uk` の付け忘れがそれだった）。
3. **実際に動かす。** 直した側と直していない側の両方を通す。
   小回りの交差点を通る経路と、本当に二段階が要る経路の両方でルートを引いて見る。
   コードを読んで「たぶん大丈夫」で終わらせない。
4. **区別は一か所で持つ形に寄せる。** 同じ判定を各所で書き直しているなら、
   `analyse()` の `needTwo` / `needKoma` のように一度だけ分け、使う側は選ぶだけにする。
5. **`./tools/check.sh` を通してから公開する。**

### 規制の種別を足す・直すときの使う側

| どこ | 何 |
| --- | --- |
| `LAYERS` | チップ（表示の出し入れ・件数）。`count` を渡せば数え方を変えられる |
| `addLayers()` | 地図のレイヤ。色・太さ・破線 |
| `expand()` | タップしたときの見出しと本文 |
| `TAG` / `GKEY` | タップしたときの種別ラベルと図。**入れ忘れると空欄になる** |
| `alertKind()` | 走行中の接近アラート。**ここに無い地点を一番近くに選ぶとアラートごと消える** |
| `analyse()` | 経路上の該当箇所（`need` / `needTwo` / `needKoma`） |
| `renderRoute()` | ルートパネルの件数と手順リスト |
| `addAvoidCandidate()` | 「二段階右折なし」候補が避ける対象 |
| `startNav()` / `renderNav()` / `navSay()` | 案内開始の音声・残り件数・曲がる瞬間の案内 |
| `drawRoutes()` / `drawRoute()` | 経路上の丸の色と文言 |
| `renderFeedback()` / `checkPassed()` | 現地確認の質問文 |
| `docs/index.html` | 凡例「地図の見方」と「この地図に入っているもの」の件数 |

`tools/audit_layers.py` はこのうち機械で見られる分（チップ・アラート・
タグ・図・凡例の件数）を突き合わせる。**経路側と文言は人が見る。**

### 逆の意味のものを同じ束に入れない

二段階右折と小回りのように**結論が逆**のものは、色を変えるだけでは足りない。
**数の束からも外す**。`needTwo`（本当に要る）と `needKoma`（打ち消される）のように
`analyse()` の中で1回だけ分け、使う側は選ぶだけにする。

## 過去の取りこぼし（同じことを繰り返さない）

- **2026-09-17** 小回りで打ち消される141か所を、地図の色だけ分けて、件数・チップ・
  経路の件数・避ける対象・読み上げは「二段階右折」のままにしていた。
  走行中のアラートが小回りの交差点で「二段階右折の交差点です」と言っていた（違反する方向の誤り）。
- **2026-09-17** 指定方向外進行禁止(2,668件)が接近アラートの一覧に無く、
  一番近くに来るたびにアラートごと消えていた（交差点手前200mで約3割が無音）。
- **2026-09-17** 二段階右折の地点に `uk` を付け忘れ、現地確認の報告欄が一度も出ていなかった。
- **2026-09-16** MapLibre の `line-dasharray` に条件式を書き、レイヤーごと作られず
  歩行者用道路1,107件が一つも出ていなかった（例外は握り潰されていた）。

## 覚えておくこと

- 記録は Obsidian の `Projects/gentukimap/`（決定ログ・懸念・監査メモ）
- データの作り直しは `tools/` の多段。市境は `data/bnd/`
- 鍵の値はリポジトリにも保管庫にも書かない
